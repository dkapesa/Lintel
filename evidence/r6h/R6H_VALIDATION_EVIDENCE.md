# R6H validation evidence

## Independent acceptance

Sonnet 5, High effort independently accepted R6H with the verdict: `ACCEPT R6H WITH NON-BLOCKING FOLLOW-UPS`; `READY TO FREEZE R6H`; `R6H READY TO FREEZE`. There are no blockers and no required corrections before freeze.

## NATIVE EXTERNAL RECORDING EVIDENCE

The externally inspected continuous Chrome recording demonstrates the real Requirements register and row selection, bounded detail without auto-opening the Inspector, explicit Requirement inspection, truthful Requirement standing, a single contextual Inspector, coherent explicit inspection/replacement behavior, no Back/breadcrumbs, no visible object-route navigation, and stable interaction.

It does not demonstrate a relationship traversal because the current real adapter data contains no genuine traversable Evidence/Finding/Requirement edge. The recording is external acceptance evidence, will be attached directly to independent Sonnet review, and is intentionally not copied or committed into the repository. R6G's large-MP4 follow-up informed this disposition.

## DETERMINISTIC / SOURCE EVIDENCE

Baseline results were R6G 11/11, R6F 14/14, R6E 8/8, R6D 23/23, and R6C 36/36. The R6H suite remains `5/5 passed` and substantively covers: (1) current-Case register grouping/order/duplicates, (2) Requirement and Finding projections, (3) selection and traversal semantics including trail/cycles, (4) duplicate targets and raw-ID non-disclosure, and (5) exact action-registry bounds.

Together with the inherited suites and source inspection, this is evidence for traversal target re-proof, primary-selection independence during traversal, relationshipTrail growth, Requirement ↔ Evidence/Finding and Finding relationships, unavailable traversal targets, raw-ID non-disclosure, Review-switch clearing, same-Review preservation, and the full four-group Requirement matrix.

The native Windows PowerShell production build succeeded against this exact candidate: Next.js 16.2.9, 47/47 static pages, exit code 0. No production source/configuration change was required.

## NOT PRODUCIBLE HONESTLY IN CURRENT NATIVE DATA

The current real adapter fixture does not expose a traversable relationship edge or every Requirement status group. No traversal, multi-hop, Finding traversal, relationship hop, or absent native group was manufactured for browser evidence. Those states remain deterministic/source evidence only.

Unavailable traversal-target behavior is source-inspected rather than covered by a dedicated executable regression case: the existing dispatch path re-proves the target, returns unavailable with unchanged state, and does not substitute a context.

## Accepted non-blocking follow-ups

- **NB-H1 — unavailable traversal-target executable regression coverage:** add a future dedicated regression case for a fabricated/nonexistent traversal target. This is a verification-hardening follow-up, not a product-correctness blocker.
- **NB-H2 — theoretical Change-context Inspector fallback:** Change contexts remain unreachable from R6H UI because Change relationships are inert; R6I owns a future Change/diff Inspector surface.

## Scope and disposition

The external recording is not present under `evidence/r6h/` or any repository path. The accepted scope remains exactly 10 existing modified paths plus 11 new paths, for 21 literal paths total. The candidate evidence document has been retired in favor of the final accepted evidence document.
