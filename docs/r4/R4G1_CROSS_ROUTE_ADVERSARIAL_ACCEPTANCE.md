# R4G.1 cross-route adversarial acceptance

R4G.1 exercised the accepted production route family as one lifecycle on 30 July 2026. It is an acceptance and bounded-correction pass, not a redesign and not completion of R4.

## Environment and isolation

The run used branch `r4g-final-quality-product-freeze`, the local production route implementations, and the isolated origin `http://r4g1.localhost:3000`. The ordinary localhost origin contained existing user records and was not mutated. One deterministic local-diff review was created, read back, reused by an identical duplicate run, traversed across routes, and cleared through the supported System control. A second deterministic disposable review, `2026-07-30T14:58:06.386Z` / `run_0145b111b8_4aa507da_c2e6e4cf`, was then created for the focused reload closeout and remains the isolated origin's one durable Report. No raw diff, external write, sample, or laboratory record became durable authority.

## Route families and ownership

- `/workspace` remains the full-bleed specialist surface for Queue, investigation, contextual Inspector, requirements, readiness, and Human Decision.
- `/home`, `/review-operations`, `/new`, and `/report` use the operational shell. Operational surfaces orient or inspect; Case File stays read-oriented.
- `/team`, `/review-policies`, `/integrations`, and `/settings` use the administrative shell with one context-aware return to Reviews and no Queue, Inspector, or Human Decision mutation.
- `/` remains public and outside the logged-in lifecycle.

The Workspace-local area rail now uses the same authoritative default destinations as the broad shell: Operations `/home` and Integrations `/integrations`. Supporting routes remain contextual destinations.

## Identity, restoration, and removal

Durable report identity is the exact Report-history `createdAt` value. Workspace, Case File, and Review Operations resolved the same `2026-07-30T13:25:00.749Z` record. Unknown URL identities failed closed and selected no substitute. Policies used the exact `policy` URL parameter and Review Operations used `selected`; presentation controls removed incompatible identity rather than selecting a replacement.

Case File `Back to Reviews` restored the exact review, Evidence mode, finding selection, and Inspector. A finding is not compatible with Requirements, so the corrected mode switch clears it and its relationship origin. After Report-history clear, restoration never resurrected the removed report. Case File names the removed identity unavailable; Workspace renders the truthful zero-history empty state without fixture substitution.

An actual full-document reload of the closeout Report exposed one acceptance defect: the exact Report remained resolved, but valid mode, selected object and Inspector presentation reset. Workspace now detects reload navigation and applies the existing validated `lintel.r4f.workspaceReturnContext.v1` restoration path. Rerunning the actual reload restored exact identity, Evidence mode, `No changed test files were detected.`, expanded Queue preference and open Inspector. Reloading unknown identity `r4g1-closeout-missing` still failed closed without selecting the valid disposable Report. The return-context schema and key are unchanged.

## Loading and unavailable authority

The genuine Workspace first paint retained known source identity while withholding recommendation, risk, blockers, readiness, and Human Decision authority. Unknown and removed targets withheld live Inspector and decision actions.

The focused closeout exercised a genuine Integrations environment-status outage by stopping the local status-serving process while preserving the already-loaded route document. Route identity, heading, static Available capabilities, and Blueprint/Export-only records remained; configured/connected claims and stale selected detail were withheld; Retry appeared instead of indefinite loading. For recovery, a disposable endpoint-only proxy forwarded the three existing status endpoints to a temporary port-3100 Next process. One activation of Retry caused exactly one GET to each endpoint and restored genuine current status without duplicate records or controls. Both temporary processes and the proxy file were removed before normal port 3000 was restored. No credential data or external write was involved.

Team storage-read denial remains not passed. The supported browser evaluator deliberately exposes neither `Storage` nor `localStorage`; its navigation policy rejected a prepared key-scoped `lintel.teamWorkspaces.v1` denial document before it loaded and explicitly prohibited an alternate workaround. The proxy and script were removed, the normal Team route again showed the same pre-existing local metadata record, and no page-side interception or mutation occurred. New Review durable-write failure/retry also remains not passed: without a supported key-scoped override for `lintel.reportHistory.v1`, no failure was asserted, no analysis was run under false interception, and no permanent source seam was added. No unsafe workaround or fabricated screenshot was used.

## Recommendation and Human Decision

The controlled review produced a Lintel recommendation of Tests required, risk 36/100 Medium, four open blocking requirements, and two missing/unverified proof records. Human Decision remained `None recorded`/pending and separate from recommendation, policy, provider, integration, and Team state. Case File displayed decision lineage but no decision or requirement mutation. Settings clear explicitly excluded the Human Decision ledger.

## Destructive history and duplicate actions

Settings named the exact `lintel.reportHistory.v1` target, one valid record, proposed zero, irreversibility, in-memory route caveat, and excluded systems/keys. Confirmation verified read-back at zero. Home, Workspace, and Case File then showed truthful post-clear states.

Duplicate-action review found and removed a second Case File `Open in Workspace` link. The retained final action owns the investigation handoff. Review Operations retains one exact Workspace link and one exact Case File link; administrative shells retain one route-level return.

## Defects and bounded corrections

1. Corrected stale Workspace area destinations for Operations and Integrations.
2. Cleared mode-incompatible Workspace selection and origin state on mode change.
3. Removed the redundant Case File Workspace handoff.
4. Converted both shared-shell document titles to a single string child, removing the React server warning.
5. Applied the existing validated Workspace return context on actual reload navigation so compatible mode, selection, Queue and Inspector state restore without requiring a query flag.

The affected scenarios and neighboring route/restoration cases were rerun. No schema, storage key, dependency, R4C laboratory, landing page, or external integration behavior changed.

## Deferred work

R4G.2 still owns broad responsive/accessibility work including the unpassed 1024×768, 768×1024, and 390×844 layouts: the browser accepted viewport override calls but attached and fresh tabs remained 936–1280 px wide. Genuine 200% zoom, screen-reader, reduced-motion, and exhaustive keyboard acceptance also remain. R4G.3 owns performance and long-session profiling. Explicit reload and complete environment-status outage recovery are passed. Team storage-read denial/recovery and safe New Review persistence-failure/retry without analysis rerun remain unexercised because the attached browser did not permit a supported key-scoped interception. R4G.4 and the remaining R4 closure work are not marked complete.
